# Common Gradle Config

The first pass keeps shared Gradle conventions in the root `build.gradle`.

This directory is reserved for extracting convention plugins or shared scripts once the build grows beyond the current MVP.
